        var radius=7;
        var calculateArea=function (radius)
        {
           return(3.14*radius*radius);
        };

        console.log("Area of the circle :" + Math.round(calculateArea(radius))) ; //Rounded OFF since desired output is 184